# Configuration du serveur SHIFT
PORT=3001
NODE_ENV=development

# PostgreSQL (Docker)
DATABASE_URL="postgresql://shift_user:shift_password@localhost:5432/shift_game?schema=public"

# Email (optionnel - sans configuration, les liens s'affichent en console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=SHIFT Game <noreply@shift-game.com>
APP_URL=http://localhost:3000